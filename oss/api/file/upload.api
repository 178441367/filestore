
type (
    UploadResp {
        Url    string `json:"url"`
        FileName string `json:"fileName"`
    }
)
@server(
    group: file
)
service oss {
    @handler Upload
    post /upload returns (UploadResp)
}
